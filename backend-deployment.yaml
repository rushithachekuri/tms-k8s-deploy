apiVersion: apps/v1
kind: Deployment
metadata:
  name: tms-backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: backend
  template:
    metadata:
      labels:
        app: backend
    spec:
      containers:
      - name: backend
        image: tms-backend
        ports:
        - containerPort: 8081
